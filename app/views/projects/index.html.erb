<h1>
<%= "#{flash[:status]}" %>
</h1>


<%= form_tag '', :method => :get do %>

   <%= select_tag "status", options_for_select([[" -- select --" , "0"]] + [["All" , "0"]] + @status)%>
  <%= submit_tag 'Filter', :class => "btn-primary btn-sm" %>
<% end %><br>


<% @projects.each do |p| %>
   <li> <%= link_to p.name, p %></li>
<% end %>
</ul>
<%= link_to "Create a New Project", new_project_path, :class => "btn-primary btn-sm" %>